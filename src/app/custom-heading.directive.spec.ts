import { CustomHeadingDirective } from './custom-heading.directive';

describe('CustomHeadingDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomHeadingDirective();
    expect(directive).toBeTruthy();
  });
});
